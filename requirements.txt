# ── Core ──────────────────────────────────────────────────────────────────────
# Pin to 4.44.x: this is the last series that ships SinkCache AND uses the
# key_cache / value_cache list API on DynamicCache.
# transformers 5.x removed SinkCache and rewrote the Cache internals.
transformers==4.44.2

torch>=2.2.0
datasets>=2.18.0
accelerate>=0.27.0
sentencepiece>=0.1.99
tokenizers>=0.19.0

# ── Evaluation metrics ────────────────────────────────────────────────────────
rouge-score>=0.1.2
nltk>=3.8.1

# ── Notebook / analysis ───────────────────────────────────────────────────────
jupyter>=1.0.0
ipywidgets>=8.0.0
matplotlib>=3.8.0
pandas>=2.2.0
seaborn>=0.13.0
tqdm>=4.66.0
numpy>=1.26.0

# ── Optional: Flash Attention 2 (CUDA only, uncomment if on a supported GPU) ──
# flash-attn>=2.5.0

# ── Optional: 4-bit quantisation on CUDA ──────────────────────────────────────
# bitsandbytes>=0.43.0
